# Shish 3 Naar — Next.js Ordering App
Run locally:
```bash
npm install
npm run dev
```
Push to GitHub:
```bash
git init
git add -A
git commit -m "Initial commit: Shish 3 Naar ordering app"
git branch -M main
git remote add origin https://github.com/<YOUR_USERNAME>/shish-3-naar.git
git push -u origin main
```
Deploy on Vercel: import your repo, set `ADMIN_KEY` and `NEXT_PUBLIC_ADMIN_KEY`, deploy.
